[package]
name = "@{name}_rhai"
version = "0.1.0"
edition = "2021"

[dependencies]
rhai = "1.21.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
@{source_pkg.name} = { path = "@{source_pkg.path}" }